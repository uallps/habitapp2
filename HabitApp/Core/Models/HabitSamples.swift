import Foundation

enum HabitSamples {
    static let defaults: [Habit] = [
        Habit(name: "Meditar", frequency: .daily),
        Habit(name: "Leer", frequency: .daily),
        Habit(name: "Correr", frequency: .weekly)
    ]
}
